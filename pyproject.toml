[project]
name = "autoresearch-maincode"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "datasets>=2.0.0",
    "tokenizers>=0.13.0",
    "tqdm>=4.0.0",
    "structlog>=23.0.0",
    "pyarrow>=12.0.0",
]